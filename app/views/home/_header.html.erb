<header class="p-3 bg-dark text-white">
  <div class="container">
    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">

      <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
        <li>
          <%= link_to 'Home', root_path, class: "nav-link px-2 text-white" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Events', events_path, class: "nav-link px-2 text-white" %>
          </li>
        <% end %>
      </ul>
      <div class="text-end">
        <% if user_signed_in? %>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-warning" %>
        <% else %>
          <%= link_to 'Sign in', new_user_session_path, class: "btn btn-outline-light me-2" %>
          <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-warning" %>
        <% end %>
      </div>
    </div>
  </div>
</header>
